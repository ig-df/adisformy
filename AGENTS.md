# Архитектурный регламент проекта (Золотой эталон)

Этот документ является обязательным стандартом и нерушимым каноном для всех последующих сессий разработки. 
Текущая версия приложения зафиксирована как **абсолютный Золотой эталон плавности, скорости, стабильности и визуального качества**. Любые последующие модификации обязаны строго соответствовать этим нормам.

---

## 💎 Главные принципы архитектуры (Фундамент)
1. **Плавность (60 FPS)**: Интерфейс работает абсолютно без рывков и просадок кадров. Любые анимации раскрытия, фильтрации и переходов строго локализованы и изолированы.
2. **Скорость и Мгновенный отклик**: Нулевые задержки при вводе в текстовые поля поиска. Поиск фильтрует данные на лету без блокировки основного потока рендеринга.
3. **Стабильность**: Надежная обработка состояния, отсутствие утечек памяти, бесконечных циклов в хуках и паразитных ререндеров.
4. **"Дорогой" и чистый визуальный стиль**: Промышленная эстетика высокого класса. Строгая светлая палитра, безупречная типографика, выверенные отступы, тактильный отклик (haptic feedback) и идеальная геометрия углов без визуального шума.

---

## 🏛️ Компонентная структура эталона
* **`App.tsx`**: Корневой оркестратор состояния (формы, комментарии, логи, заказчики, активные сессии, фильтры, управление модальными окнами, тосты). Мемоизированные хэндлеры (`useCallback`, `useMemo`).
* **`Header.tsx`**: Верхняя панель с логотипом завода, скользящим аппаратным индикатором табов (`transform: translateZ(0)`), статусом синхронизации, индикатором онлайна и доступом суперпользователя.
* **`SearchTab.tsx`**: Сверхбыстрый интерактивный поиск пресс-форм с чистым главным экраном, подсветкой найденных совпадений и интерактивной карточкой отсутствия с быстрой кнопкой добавления формы (`Přidat formu`).
* **`DatabaseTab.tsx`**: Полная табличная база данных с сортировкой, фильтрацией, монохромными пустыми состояниями категорий и возможностью раскрытия детализированного аккордеона по каждой форме.
* **`DashboardTab.tsx`**: Статистическая аналитическая сводка с карточками распределения по цеховым зонам/статусам и эталонным экраном «Vše v pořádku».
* **`MoldRow.tsx`**: Высокопроизводительный компонент строки таблицы с изолированным раскрывающимся содержимым без вызова layout thrashing.
* **`Modals.tsx`**: Набор изолированных диалоговых окон (редактирование позиции, заказчика, полный редактор, комментарии, просмотр фото/PDF чертежей, экспорт/импорт Helios, корзина, логи).

---

## 🚫 Строгие запреты (Профилактика лагов и фризов)
1. **Запрет на анимации и transitions на тегах `tr` и `tbody`**: 
   - Категорически запрещено вешать `transition` на строки таблицы `tr` или другие табличные контейнеры. Это приводит к глобальному пересчету геометрии страницы (Layout Thrashing) при раскрытии строк.
2. **Изоляция анимации аккордеона**:
   - Раскрытие информации внутри строки выполняется исключительно через внутренний контейнер ячейки с CSS Grid техникой (`grid-template-rows: 0fr -> 1fr`) и аппаратным ускорением (`opacity`, `transform`).
3. **Тактильный микро-отклик**:
   - Интерактивные кнопки и плашки обладают деликатной микро-анимацией сжатия `:active { transform: scale(0.96); opacity: 0.85; }` для мгновенного подтверждения клика пользователю.
4. **Контроль ререндеров**:
   - Запрещено передавать немемоизированные тяжелые вычисления в тело рендера. Зависимости в `useEffect` должны быть строго примитивами или стабильными ссылками.
5. **Чистота и сдержанность пустых экранов (Empty States)**:
   - Главный экран поиска остаётся чистым и строгим.
   - Экраны пустых результатов используют строгую монохромную заводскую эстетику (тонкие штрихи `1.75px`, мягкий фон `#f1f5f9`, отсутствие цветного шума).
6. **Регламент безопасного каскада списков (Safe Cascade)**:
   - Каскадное появление ограничивается исключительно первыми 8 строками (`idx < 8`) с мягкой задержкой 34 мс и плавным ходом 0.22s (translateY: 6px -> 0), что даёт выверенный кинематографичный эффект без рывков.
   - Каскад обладает мгновенным сбросом (`Zero-Latency Touch Guard`): при любом касании экрана, старте скролла или свайпа плитки анимация мгновенно снимается, исключая залипание жестов и сбои расчёта координат `offsetTop`.
7. **Разграничение жестов и двухпороговый свайп (Two-Stage Left Swipe & Disambiguation)**:
   - Долгий тап на строку формы на мобильных устройствах зарезервирован исключительно под тактильный подъем плитки (`swipe-armed`) и запуск свайп-действий:
     - **Свайп вправо**: открывает модальное окно добавления комментария («Přidat poznámku»).
     - **Свайп влево (двухпороговый с упругим сопротивлением)**:
       - *Средний свайп* (от 70px): раскрывает и активирует иконку шестерёнки **«Upravit»** с тактильным щелчком (`haptic.threshold()`). При отпускании открывает карточку редактирования формы.
       - *Глубокий затянутый свайп* (от 210px с динамическим механическим сопротивлением и увеличенным отступом иконок): раскрывает вторую контрастную красную пилюлю **«Přesunout do koše»** (появляется только после 130px намеренного смещения) с плотным haptic-импульсом (`[40]`) и передачей приоритета. При отпускании запускает перемещение формы в корзину (с модальным подтверждением). Исключает любые случайные срабатывания.
   - Вызов контекстного меню на мобильных/сенсорных устройствах полностью заблокирован, что исключает конфликт жестов, прерывание свайпа и случайные нажатия. На ПК при этом сохраняется привычный клик правой кнопкой мыши.
   - Также действие перемещения в корзину сохранено в модальном окне редактирования («Upravit formu» → кнопка «Přesunout do koše») и в режиме детального редактирования формы в аккордеоне.

8. **Регламент интерактивного плана смены (Shift Plan Dynamic Animation & Cart Fly)**:
   - При пакетном добавлении распознанных форм в сменный план из Gemini Vision сканера, окно сканера закрывается и плавно исчезает с экрана без задержек (`closeNumberScanner()`).
   - Ровно после закрытия сканера (320 мс) запускается выверенная кинематографическая дуговая анимация частицы карточки («+X do plánu»): чисто-белый монохромный фон с тонким заводским контуром (`1px solid #e2e8f0`), контрастным темно-графитовым текстом и изумрудно-зелёной пиктограммой смены.
   - При приземлении карточки в плавающую кнопку смены запускается чистый физический пружинный отскок (`shiftBtnBounceJump`: сжатие → прыжок вверх → упругая стабилизация) без паразитных затемняющих прямоугольных теней (`box-shadow`), сопровождаемый сдвоенным тактильным импульсом (`[30, 45]`) и обновлением счетчика бейджа.
   - В модальном окне «Seznam forem na směnu» теги заказчиков (`.tag-customer`) строго сохраняют свой индивидуальный брендовый цвет фона и рамки, не обесцвечиваясь при вычеркивании выполненных позиций.

9. **Регламент системы аудита и личной статистики (Audit & Personal Shift Activity)**:
   - Общий глобальный журнал (`mold_logs`) и история базы данных сфокусированы строго на ключевых производственных операциях (создание формы, смена позиции, изменение статусов, наводки, фото, заметки).
   - Фоновые микро-события (поисковые запросы, сканирование камеры, открытие карточек) не засоряют историю.
   - Завершение задач в плане смены (*«Odebráno z regálu»*) фиксируется адресно и сохраняется в личный кабинет наладчика (*«Kabinet seřizovače»* → *«Dnes zpracováno»* / *«Zpracované formy»*), не перегружая глобальный публичный список перемещений.
   - Для суперпользователя в панели активности сотрудников доступен полный мониторинг ключевых действий персонала, включая авторизацию и прогресс по сменному плану (*KPI «Plán směny»*).

10. **Регламент интерактивной карты цеха (Interactive Factory Map, Centered Mobile Canvas & Multi-Device Realtime Sync)**:
    - **Позиционирование зон**: Секция «MANIPULAČNÍ ZÓNA» расположена в центральном ряду непосредственно над кнопкой станка **11**. Сверху над зоной сохранён свободный поперечный проход цеха со стрелками.
    - **Мобильный приоритет и адаптивность (@media max-width: 640px — Главный производственный интерфейс)**: Мобильная версия является основным рабочим инструментом цеха. Карта цеха сохраняет естественные ПК-пропорции станков, центрируется по ширине (`width: 100% !important; max-width: 100vw; overflow-x: hidden`), оптимизирована через 3-колоночную сетку `minmax(0, 1fr)`. В мобильной карточке станка гармонично отображается название выпускаемой детали (`.mach-mold-product-name` с микро-типографикой `0.52rem` и `ellipsis`) рядом с контрастным номером формы.
    - **Трехуровневое разграничение рабочих статусов ТПА**:
      - 🟢 **Зеленый («V chodu / V pořádku»)**: Станок в работе, выпуск продукции в штатном режиме без замечаний.
      - 🟠 **Оранжевый («V chodu s problémy / Nutný servis»)**: Станок **НЕ остановлен**, продолжает работу во второй/третьей смене, но имеет замечания или требует планового сервиса.
      - 🔴 **Красный («Odstaveno / Mimo provoz»)**: Станок **полностью остановлен и выведен из производства** (авария, критическая поломка, ожидание запчастей).
      - Выбор статуса наладчиком имеет абсолютный приоритет и не перетирается автоматикой.
    - Трехколоночная структура промышленной расстановки термопластавтоматов: левый ряд `[34..10]`, центральный ряд с манипуляционной зоной и блоком `[11..31]`, правый ряд `[36..18]` и `[29..7]`.
    - Блок станков `[29..7]` позиционируется строго на нижнем уровне цеха, формируя единую горизонтальную базовую линию с соседними нижними рядами `[11..31]` и `[37..10]`.
    - Поперечный транспортный проход между верхним (`[36..18]`) и нижним (`[29..7]`) блоками станков с гибким спейсером (`flex: 1`), центрированным маркером проезда и жёлтой стрелкой направления исключает слипание карточек и обеспечивает идеальный баланс свободного пространства.
    - Карточки станков с монохромной окантовкой, точной цветовой индикацией рабочих статусов (Зеленый — В порядке, Янтарно-желтый — Сервис, Красный — Ошибка/Замечания), компактным бейджем установленной формы и мгновенной реакцией на клик.
    - **Межстаночные проходы и навигационные маркеры (Slate CAD-стиль)**: Между термопластавтоматами в колоннах и в транспортных коридорах интегрированы проходы с нейтральным инженерным графитовым штрихпунктиром (`#94a3b8`), компактными маркерами (`#f1f5f9` с рамкой `#cbd5e1`) и стрелками прохода персонала (`#64748b` / `⟷`), полностью изолированными от технологических статусов станков, адаптированными под высоту экрана ПК без вертикального скролла и под мобильные экраны.
    - **Мультиплатформенная сквозная Realtime-синхронизация**: Состояние всех 29 прессов (статус готовности, установленная форма, журнал проблем и сервиса) реплицируется в облачную базу Supabase (`app_settings` / `adis_factory_machines_state_v1`). Изменения, внесенные наладчиком на смартфоне или планшете, мгновенно без перезагрузки отображаются на всех экранах операторов в цехе.

---

## 🎨 Фирменная заводская идентификация позиций
Цветовая дифференциация цеховых зон строго регламентирована:
* **Mezisklad**: `#4b5563` (Нейтральный стальной)
* **Z (Zóna Z)**: `#16a34a` (Изумрудный зеленый)
* **C (Zóna C)**: `#dc2626` (Предупредительный красный)
* **M (Zóna M)**: `#2563eb` (Инженерный синий)
* **B (Zóna B)**: `#ffffff` с рамкой `#cbd5e1` и темным текстом `#0f172a`
* **H (Zóna H)**: `#9a3412` (Терракотовый)
* **Externí**: `#7c2d12` (Глубокий коричневый)

---

## 🔐 Ролевая модель и безопасность (RBAC)
В приложении внедрено строгое разделение прав доступа (Role-Based Access Control):

1. **Гость (Неавторизованный пользователь)**:
   - Строго режим «Только просмотр и поиск»: доступен поиск форм, табличная база данных, аналитика и схема цеха.
   - Любая редактура заблокирована: изменение статусов станков, привязка форм на карте цеха, журнал дефектов, управление задачами сменного плана, создание и редактирование форм мгновенно вызывают окно авторизации.
   - Сканер с искусственным интеллектом (Gemini Vision AI / камера) строго отключен для гостей во избежание нецелевого расхода бесплатных токенов. Вызов сканера требует входа в систему.
2. **Оператор (Авторизованный пользователь)**:
   - Полный доступ к поиску, ИИ-сканеру, сменному плану, карте цеха, комментированию, загрузке фото, изменению статусов и позиций (Mezisklad, Zóna H и т.д.).
   - Права на добавление новых наводок (`+ Přidat návodku`).
   - Строго заблокированы деструктивные действия: удаление и замена существующих наводок, редактирование названий, перемещение форм в корзину (через свайп, кнопку или модальное окно).
3. **Суперпользователь (`isSuperUser = true`)**:
   - Полные административные права. Разблокирована перезапись и удаление («Nahradit», «Smazat») текущих наводок в базе.
   - Разблокировано удаление форм в корзину, глубокий свайп удаления, прямое изменение названий деталей.
   - Полный доступ к панели инспекции активности сотрудников с разбивкой по операциям и сменному плану.

---

### 📅 Статус эталонов и версионирование
* **Ветка 1 (Классический монохромный эталон)**: `index.html.golden_backup_v39` — строгий инженерный монохром со стальной CAD-разметкой.
* **Ветка 2 (Цветной эталон с солнечной разметкой)**: **Зафиксирован (Золотой эталон v5.4 — Яркая разметка Sunbeam Yellow `#ffd000` / `#ffbb00`, минималистичные векторные стрелки, резервная копия `index.html.golden_backup_v40`)**.
* **Дата фиксации**: 22 сентября 2026 года.
* **Архитектурный вердикт**: Полная плавность 60 FPS, гармоничная солнечная разметка цеха Sunbeam Yellow, автономные тактильные кнопки станков с четким физическим зазором (Air Gap) и микро-тенями, 100% идентичность ПК и мобильной версии, трехуровневая индикация готовности ТПА (V chodu / S problémy / Odstaveno), Realtime-синхронизация состояния всех 29 прессов через Supabase, трехуровневый RBAC и тактильный двухпороговый свайп. Полная готовность к промышленной эксплуатации и дальнейшему редактированию.
